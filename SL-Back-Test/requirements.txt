# FastAPI Core
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Async Database (PostgreSQL)
asyncpg==0.29.0  # PostgreSQL용 비동기 드라이버 - 매우 빠름
sqlalchemy[asyncio]==2.0.25  # 비동기 ORM
alembic==1.13.1  # 데이터베이스 마이그레이션

# Data Processing (대용량 데이터 최적화)
pandas==2.2.3  # 데이터 분석 및 조작 (Python 3.13 호환)
numpy==2.0.2  # 수치 계산 (Python 3.13 호환)
polars==1.15.0  # Pandas보다 10-100배 빠른 DataFrame 라이브러리 (Rust 기반)
pyarrow==18.1.0  # Apache Arrow - 컬럼형 데이터 처리 (메모리 효율적)
# dask[complete]==2024.1.0  # 대용량 데이터 병렬 처리 (10GB+ 데이터용) - Python 3.13 호환성 문제로 임시 제거

# Caching (성능 최적화)
redis==5.0.1  # 캐싱 및 세션 관리
aiocache==0.12.2  # 비동기 캐싱

# Database Connection Pooling
psycopg[binary]==3.1.18  # PostgreSQL 어댑터 (Python 3.13 호환)

# 백테스팅 및 퀀트 분석
# vectorbt==0.26.1  # 백테스팅 프레임워크 - numpy 호환성 문제로 임시 제거
# ta-lib==0.4.28  # 기술적 지표 라이브러리 (C 라이브러리 필요, 선택사항)
# quantstats==0.0.62  # 퀀트 성과 분석 - 의존성 문제로 임시 제거

# API 유틸리티
python-multipart==0.0.6  # 파일 업로드
python-jose[cryptography]==3.3.0  # JWT 토큰
passlib[bcrypt]==1.7.4  # 비밀번호 해싱
python-dotenv==1.0.0  # 환경변수 관리

# 로깅 및 모니터링
loguru==0.7.2  # 향상된 로깅
prometheus-fastapi-instrumentator==6.1.0  # Prometheus 메트릭

# 데이터 검증
# pandera==0.18.0  # DataFrame 스키마 검증 - pandas 호환성 문제로 임시 제거

# 유틸리티
httpx==0.26.0  # 비동기 HTTP 클라이언트
orjson==3.9.12  # 빠른 JSON 직렬화 (Python json보다 2-3배 빠름)
ujson==5.9.0  # 빠른 JSON 파싱

# 테스팅
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0  # 테스트용 HTTP 클라이언트

# 타입 체킹
mypy==1.8.0

# 개발 도구
black==24.1.1  # 코드 포맷팅
isort==5.13.2  # import 정렬
flake8==7.0.0  # 린팅
